"use client";

export default function NotFound({ error }) {
  return (
    <main className="not-found">
      <h1>Meal not found</h1>
      <p>Unfortunately, we could not found the requested page or meal data.</p>
    </main>
  );
}
